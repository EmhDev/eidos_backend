class FileMetadataService:
    def analyze(self, file_path: str) -> dict:
        # Placeholder (futuro: detectar con librerías específicas)
        return {
            "creator": "unknown",
            "created_at": "unknown"
        }
